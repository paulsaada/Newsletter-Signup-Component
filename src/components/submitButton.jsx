import React, { Component } from 'react';

class submitButton extends Component {
   state = {}
   render() {
      return (
         <div>
            <button>NEXT</button>
         </div>
      );
   }
}

export default submitButton;